
# TIL 标签体系说明

## 概述

本 TIL 笔记采用标签驱动的组织方式，而非传统的文件夹分类。
所有笔记文件都存放在根目录，通过标签来实现分类、检索和关联。

**设计原则：标签是服务记录的，不能让标签妨碍记录。**

## 快速捕获规则

对于灵感迸发或随手记录的场景，最低要求：

```
#未分类

笔记内容...
```

一个领域标签即可，其他标签等整理时再补充。不要因为纠结分类而放弃记录。

---

## 标签结构

### 1. 领域标签（必需）

用于标识笔记所属的主题领域，每个笔记必须包含至少一个领域标签。

| 标签 | 说明 | 适用内容 |
|------|------|----------|
| `#编程技术` | Swift、iOS开发、编程思维相关 | 代码技巧、技术概念、开发工具 |
| `#认知科学` | 心理学、思维模型、认知偏差 | 心理学概念、思维模型、行为模式 |
| `#设计思维` | UI/UX、产品设计、视觉设计 | 设计原则、案例分析、设计理论 |
| `#历史知识` | 历史事件、历史人物、历史思考 | 历史事实、历史观点、时代背景 |
| `#个人管理` | 时间管理、人际关系、工作方法 | 效率技巧、沟通方法、管理原则 |
| `#语言学习` | 语言词汇、语法、文化背景 | 外语词汇、语言现象、文化差异 |
| `#学习方法` | 学习技巧、记忆方法、知识管理 | 学习策略、记忆技巧、笔记方法 |
| `#生活窍门` | 生活技巧、实用工具、日常便利 | 生活技巧、工具使用、实用指南 |
| `#自然观察` | 自然现象、科学原理、生态观察 | 自然现象、科学知识、生态思考 |
| `#工具技巧` | 软件工具、AI工具、效率工具 | 工具使用、技巧分享、配置方法 |
| `#未分类` | 临时存放，待后续整理 | 无法确定领域的内容 |

**规则：**
- 第一个领域标签是主分类
- 允许添加第二个领域标签作为辅助（如 `#认知科学 #个人管理`）
- 如果实在无法确定领域，使用 `#未分类`
- 每季度统计 `#未分类` 下的主题，重复出现 3 次以上的主题可考虑新增为正式领域标签

### 2. 类型标签（必需）

用于标识笔记的内容类型，每个笔记必须包含一个类型标签。

| 标签 | 说明 | 特征 |
|------|------|------|
| `#概念` | 理论、定义、模型、原理 | 解释性内容，理论性强 |
| `#技巧` | 实用操作方法、具体步骤 | 可操作性强，有明确步骤 |
| `#洞察` | 深度思考、感悟、启发 | 个人思考，观点鲜明 |
| `#资源` | 参考资料、链接、收藏 | 外部资源，值得保存 |
| `#未分类` | 临时存放，待后续整理 | 无法确定类型的内容 |

### 3. 状态标签（可选）

用于标识笔记的完成状态。

| 标签 | 说明 | 使用场景 |
|------|------|----------|
| `#完成` | 内容完整，可直接使用 | 已完善的笔记 |
| `#草稿` | 初步记录，需要完善 | 未完成的思考 |
| `#待整理` | 需要进一步整理和分类 | 待优化的内容 |

### 4. 评级标签（可选）

`#精华` 作为独立评级标签，用于标识特别有价值的内容。

**示例：**
- `#草稿 #精华` → 值得优先完善的灵感
- `#完成 #精华` → 最优质的知识资产

### 5. 关系标签（可选）

用于标识笔记的应用状态。

| 标签 | 说明 | 使用场景 |
|------|------|----------|
| `#实践` | 已在生活中应用 | 经过验证的内容 |
| `#待研究` | 需要深入了解 | 值得进一步探索 |

**关于笔记关联：** 建议使用 Obsidian 双链 `[[笔记名]]` 代替 `#相关` 标签，在正文末尾添加「相关笔记」区块：

```markdown
## 相关笔记
- [[快速记忆法]]
- [[间隔重复是什么]]
```

## 标签格式规范

### 标准格式
```
#领域标签 #类型标签 #状态标签（可选）#评级标签（可选）

笔记内容...
```

### 最简格式（快速捕获）
```
#未分类

笔记内容...
```

### 完整示例
```markdown
#编程技术 #技巧 #完成

原本以为添加一定高度的 `.frame` 就能增加可点区域，岂料不行。
## 1. `.frame` 只影响布局，不保证可点击
`.frame(width:height:)` 只是调整视图的布局和渲染区域...
```

### 跨领域示例
```markdown
#认知科学 #个人管理 #洞察 #完成

为什么人会对彩票上瘾？这既是认知偏差，也涉及个人决策管理...
```

### 快速捕获示例
```markdown
#未分类

今天看到一个关于蜜蜂集体决策的现象，很有趣。蜜蜂通过"摆尾舞"来投票决定新蜂巢的位置...
```

### 格式要求
1. 标签行必须是文件的第一行
2. 标签之间用空格分隔
3. 标签行后必须有一个空行
4. 标签以 `#` 开头，不包含其他符号
5. 标签使用中文，便于理解
6. 领域标签在前，类型标签次之，状态和评级标签可选

## 使用指南

### 快速捕获（推荐优先使用）

当灵感迸发或随手记录时：
1. 写下标题和内容
2. 至少添加一个领域标签（不确定就用 `#未分类`）
3. 保存，完成

**不要纠结类型、状态、是否整理，先记下来再说。**

等某天回看时，再补充完整标签。

### 完整标注（整理时使用）

#### 步骤
1. 分析笔记内容，确定所属领域
2. 判断笔记的内容类型
3. 评估笔记的完成状态
4. 判断是否值得标记为精华
5. 按照标准格式添加标签

#### 选择标签的原则
- **领域标签**：根据笔记的核心主题选择，允许跨领域
- **类型标签**：根据笔记的内容性质选择
- **状态标签**：根据笔记的完善程度选择（可选）
- **精华标签**：特别有价值的内容（可选）

#### 示例
```
笔记内容：关于如何使用 iPhone 拍摄月亮的技巧
领域标签：#生活窍门（实用技巧）
类型标签：#技巧（具体操作方法）
状态标签：#完成（内容完整）
结果：#生活窍门 #技巧 #完成
```

#### 跨领域示例
```
笔记内容：为什么人会对彩票上瘾
领域标签：#认知科学 #个人管理（跨领域）
类型标签：#洞察（深度思考）
状态标签：#完成
结果：#认知科学 #个人管理 #洞察 #完成
```

### 维护和更新

#### 定期检查
- 每月检查一次 `#未分类` 标签使用情况
- 将重复出现的主题整理为新的领域标签
- 及时更新状态标签（如从 `#草稿` 改为 `#完成`）

#### 标签调整
- 当笔记内容有重大更新时，考虑调整类型标签
- 当发现更合适的领域标签时，及时调整
- 状态标签应随笔记完善程度及时更新

#### 批量处理
使用以下命令批量检查标签：
```bash
# 检查没有标签的文件
for file in *.md; do
  if ! head -1 "$file" | grep -q "^#"; then
    echo "$file"
  fi
done

# 统计各标签使用情况
grep -h "^#" *.md | sort | uniq -c | sort -rn

# 查找所有 #未分类 笔记
grep -l "^#未分类" *.md
```

## 检索和查找

### 按领域查找
```bash
# 查找所有编程技术相关笔记
grep -l "^#编程技术" *.md
```

### 按类型查找
```bash
# 查找所有技巧类笔记
grep -l "#技巧" *.md
```

### 组合查找
```bash
# 查找编程技术类的技巧笔记
grep -l "^#编程技术.*#技巧" *.md
```

### 在 Obsidian 中使用
- 使用标签面板：`#编程技术`
- 使用搜索功能：`tag:#编程技术`
- 创建标签视图：按标签分组查看

## 标签扩展

### 添加新领域标签
当现有领域标签无法覆盖新内容时：
1. 先评估是否可以归入现有标签
2. 如需新增，保持命名风格一致
3. 更新本文档中的标签表
4. 考虑是否需要调整现有笔记的标签

### 添加新类型标签
当发现新的内容类型时：
1. 确保与现有类型标签不重复
2. 保持标签简洁明了
3. 更新标签说明文档

## 常见问题

### Q: 一个笔记可以有多个领域标签吗？
A: 可以。第一个是主领域，第二个是辅助领域。如 `#认知科学 #个人管理`。

### Q: 状态标签什么时候需要更新？
A: 当笔记的内容有重大改进或完善时，应及时更新状态标签。从 `#草稿` 到 `#完成` 是最常见的更新路径。

### Q: 如何处理跨领域的笔记？
A: 添加多个领域标签，第一个为主分类。也可以在正文末尾用双链关联相关笔记。

### Q: 标签顺序有要求吗？
A: 建议按照「领域标签 → 类型标签 → 状态标签 → 精华标签」的顺序，便于阅读和维护。

### Q: 什么时候用 `#未分类`？
A: 快速记录时不确定领域或类型，先用 `#未分类` 保存，整理时再补充。

### Q: 如何表达笔记之间的关联？
A: 使用 Obsidian 双链 `[[笔记名]]`，在正文末尾添加「相关笔记」区块。避免使用模糊的 `#相关` 标签。

## 交接指南

### 新接手人员需要了解
1. 标签体系的结构和含义
2. 标签格式规范
3. 如何为新笔记添加标签
4. 如何维护和更新标签

### 快速上手
1. 阅读本文档
2. 查看现有笔记的标签使用示例
3. 尝试为几个新笔记添加标签
4. 熟悉标签检索方法

### 注意事项
- 保持标签使用的一致性
- 不要随意创建新标签
- 定期维护标签体系
- 及时更新文档

## 版本历史

- v1.1 (2026-08-08): 降低标签门槛，增加快速捕获规则，允许跨领域，`#精华` 独立为评级标签，用双链代替 `#相关`
- v1.0 (2026-08-08): 初始版本，建立基础标签体系
